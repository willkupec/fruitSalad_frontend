import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#0096DC",
    },
    secondary: {
      main: "#FFA600",
    },
    background: {
      default: "#0096DC"
    },
  },
})

export default theme