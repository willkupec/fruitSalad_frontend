import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#FFA600",
    },
    background: {
      default: "#000000"
    },
  },
})

export default theme